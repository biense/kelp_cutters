<nav class="navbar navbar-light bg-light">
    <div class="d-flex flex-row form-inline">
        <div class="d-flex align-items-center btn btn-primary btn-sm">
            <%= link_to "All articles", root_path %>
        </div>

        <div class="d-flex align-items-center", style="padding-left: 5px; padding-top: 15px;">
            <%= form_tag articles_path, method: :get do %>
                <p>
                <%= text_field_tag :query, params[:query] %>
                <%= submit_tag "Search Articles", name: nil, :class => "btn btn-primary btn-sm" %>
                </p>
            <% end %>
        </div>
    </div>
</nav>
